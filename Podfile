# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Mixer-replica' do
  # Pods for Mixer-replica
  
  pod 'AFNetworking'
  pod 'JSONModel'
  pod 'SDWebImage'
end
